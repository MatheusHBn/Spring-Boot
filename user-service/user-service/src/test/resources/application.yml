spring:
  jpa:
  sql:
    defer-datasource-initialization: true
    init:
      mode: always
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        globally_quoted_identifiers:  true